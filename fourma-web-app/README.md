# Fourma Web MVP design prototype

Interactive, responsive front-end prototype based on `Product/web-mvp-functional-specification.md`.

Open `index.html` directly or run:

```bash
python3 -m http.server 4173 --directory fourma-web-app
```

Then visit `http://localhost:4173`.

Key flows: landing → AI interview → location intelligence and property preview.
